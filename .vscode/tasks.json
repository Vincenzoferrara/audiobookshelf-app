{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Debug task",
      "type": "shell",
      "command": "npm install && npm run generate && npm run build && npx cap sync && ionic cap run --external"
    }
  ]
}
