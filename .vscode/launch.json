{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "android",
      "request": "launch",
      "name": "Debug",
      "appSrcRoot": "${workspaceFolder}/android/app/src/main",
      "apkFile": "${workspaceFolder}/android/app/build/outputs/apk/debug/app-debug.apk",
      "preLaunchTask": "Debug task", // Use the new task name
      //"gradleTask": ":app:assembleDebug", // This will build the APK
      "adbPort": 5555 // Ensure this is not conflicting
    }
  ]
}
